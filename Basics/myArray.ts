

const heros:string[]= []

heros.push("spiderman")

const heros_power:number[]= []

heros_power.push(1)

const heros_level:Array<number>= []

type User= {
    name:string
}

const allUsers:Array<User>=[]

allUsers.push({name:"nishtha"})  

const Mlmodels: number [][]=[   // when we want numbers in arrays inside array

]

export{}

